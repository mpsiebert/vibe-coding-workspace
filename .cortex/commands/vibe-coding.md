---
description: Start the Vibe Coding booth workflow
---

$vibe-coding

Start the Vibe Coding 2.0 booth workflow for the attendee now.

This workflow is local-only. Do not ask whether the attendee wants to publish, release, or move the app beyond localhost, and do not call or search for publishing tools. The goal is a working Streamlit app at http://localhost:8501.

Follow the `vibe-coding` skill playbook exactly from Phase 1. Use the `vibe-coding-mcp` MCP tools at each phase gate, and never simulate MCP tool outputs.

Additional context from the command invocation, if any:

$ARGUMENTS
